import React from "react";

function ActiveButton() {
    return (
        <div className="submit-box">
            <button type="submit" className="btn btn-submit btn-active">Отправить</button>
        </div>
    )
}

export default ActiveButton;